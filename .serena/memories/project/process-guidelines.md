# Project Process Guidelines

- `AGENTS.md` is the authoritative local instruction file.
- User-facing replies use the `caveman` skill unless the user says `stop caveman` or `normal mode`.
- Beads (`bd`) is initialized with prefix `pagebound`; default to creating or claiming a bead before code/docs/config/test/workflow changes. Use `bd prime` for workflow context. Closed setup beads so far: `pagebound-a4p`, `pagebound-444`.
- MCP guidance in `AGENTS.md`: use Godot AI MCP at `http://127.0.0.1:8000/mcp` for live Godot editor work; use Serena for memories and targeted searches, with limited GDScript semantic support; use `rg` and direct reads for GDScript implementation details.
- Git workflow in `AGENTS.md`: use Conventional Commits for commit messages and Conventional Commit-style branch names such as `feat/feed-timer`, `fix/media-persistence`, `chore/update-agent-rules`, or `docs/workflow-guide`; do not create branches with the `codex/` prefix; before completion run `git status --short --branch`; summarize changed files and checks not run; update Serena memories when durable project facts change.
- Brooks-Lint guardrails in `AGENTS.md`: name the likely decay risk before adding behavior, avoid adding feature/business logic to files over 800 lines, extract helpers when adding >50 lines to files over 500 lines, split mixed-responsibility functions over 20 lines, prefer typed input objects over helpers with >4 parameters, and report large-file growth before completion.
- Current template port branch was renamed from `codex/claude-studios-port` to `codex-game-studios-port` to satisfy the no-`codex/` prefix rule.

- 2026-05-10 onboarding/adoption setup: root GDD is `PAGEBOUND_CODEX_GDD_v1_5.md`; review mode is `lean`; project stage is `Systems Design`; engine is pinned to Godot 4.6.2 with GDScript in `AGENTS.md`, `.codex/docs/technical-preferences.md`, and `docs/engine-reference/godot/VERSION.md`.
- 2026-05-10 generated workflow artifacts: `production/project-stage-report.md`, `docs/adoption-plan-2026-05-10.md`, `design/gdd/game-concept.md`, `design/gdd/systems-index.md`, `production/stage.txt`, and `production/session-state/active.md`.
- 2026-05-10 `Godot Project Shell` GDD is approved in `design/gdd/godot-project-shell.md`; review history lives at `design/gdd/reviews/godot-project-shell-review-log.md`.
- 2026-05-10 `Godot Project Shell` implementation exists: `project.godot` points to `res://Main.tscn`; `Main.tscn` has the approved canonical shell tree, hidden CanvasLayer UI slots, primitive visible paper `PageGround`, `Camera3D`, `DirectionalLight3D`, and `WorldEnvironment`; shell code lives in `src/shell/`; smoke validation lives at `tests/smoke/shell_smoke_check.gd`.
- 2026-05-10 Godot AI addon was copied from `https://github.com/hi-godot/godot-ai` into `addons/godot_ai/` and enabled in `project.godot`. `uv` is available at `/home/seanlongcc/.local/bin/uv`; headless editor reports `MCP | plugin disabled in headless mode`, so live MCP at `http://127.0.0.1:8000/mcp` requires opening the Godot editor with the plugin active.
- 2026-05-10 asset direction after shell: move away from hand-drawn toward cohesive cute top-down pixel fantasy sprites/cards presented as Godot 3D `Sprite3D`/card-like objects on a lit finite paper diorama. Source pool is itch.io game assets; do not import assets until source URL, creator, license, cost, and allowed commercial use are recorded.
- 2026-05-10 local asset source library lives outside the repo at `C:\Users\seanl\Documents\Godot\assets` (`/mnt/c/Users/seanl/Documents/Godot/assets`), about 1.4 GB / 88,427 files. Inventory note: `production/assets/local-asset-library.md`. Keep it external; import only curated subsets into repo asset folders with provenance and license metadata.
- 2026-05-11 MVP Systems Design gate passed. `design/gdd/systems-index.md` tracks 29 systems with all 19 MVP systems approved and `MVP systems designed` at `19/19`. Cross-GDD review: `design/gdd/reviews/mvp-systems-design-review-2026-05-11.md`. Gate report: `production/gates/systems-design-to-technical-setup-2026-05-11.md`. `production/stage.txt` is now `Technical Setup`. Next practical build step is Resource Data Schemas implementation, followed by Input/Rebinding, Player Controller/Dash, Camera/Lighting, Runtime Event Bus, and Damage Model.
- 2026-05-11 first playable MVP runtime foundation on `feat/mvp`: `Main.tscn` boots a `FirstPlayableRuntime` child under `RunRoot`; runtime creates `RuntimeEventBus`, player `CharacterBody3D`, camera follow service, damage model/health components, pooled `Label3D` damage numbers, scene-owned `RunDirector`, two prototype enemy families (`inkling_chaser` slow/sturdy and `paper_scrap_swarmer` fast/weak), one `Waxlight Comet` auto weapon, visible HP/XP/enemy/time HUD, collectible Color Mote XP drops with magnet pull range, 50 player HP, 5 enemy contact damage, finite page bounds clamp, finite spawn bounds, and primitive Pagecraft mark deposit/activation. XP awards only on pickup collection. Dead enemies visibly despawn, stop physics, disable collision, and stop being targetable. `move_up`/W maps toward negative Z/page top. No third-party assets imported; all visuals are primitive placeholders. Smoke checks now cover shell, schema, movement, camera, damage model, damage numbers, combat prototype, XP pickup/magnet, run director spawning/budget, enemy lifecycle/families, HUD counters, page bounds, Pagecraft, and first playable integration.
- 2026-05-11 first 5-minute vertical slice readability/scaling pass on `feat/mvp`: dash-activated Waxlight marks become finite active zones, inactive Waxlight marks expire plus cap enforcement, Waxlight dash pulse visuals clean up, and Star Sticker Swarm reads as orbit -> fire -> page-stick -> pop -> reform with DamageModel hit/pop damage numbers and finite cleanup. `RunUpgradeState` uses chunky prototype upgrades: Waxlight damage +2, active duration +1s, unactivated mark cap +3, cooldown -0.25s, player max HP +20, Star Sticker damage/count level-up, and Candle Spark +15% steps. `RunLevelTracker` owns prototype thresholds `[3, 6, 10, 15, 21, 28]`; level-up opens a paused 3-choice centered/equal-card draft in `LevelUpScreen`; layout is smoke-tested at 960x540 through 1920x1080. `PageEventController.first_event_time_seconds = 60.0` for prototype validation while full-run GDD Page Event timing remains 5:00; 5:00 summary remains. `RunDirector` has early pressure bands (`opening`, `first_pressure`, `ink_surge`, `page_crush`); finite page half-extents are 11.0 x 7.0 with a 22 x 14 page mesh; Color Mote magnet radius is 8m. Retry after death snaps/rebinds the camera to the new player. Current counts: 33 GDScript source files, 26 smoke checks, 7 ADRs. No third-party assets imported; primitive placeholders only.
