# First 5-Minute Slice Facts (updated 2026-05-15)

- Branch `feat/mvp` implements the first polished exemplar package plus the user-approved second content batch. Starter state remains Waxlight Comet level 1 and Dog active, with no passive owned by default.
- Current playable weapon pool is `waxlight_comet` and `star_sticker_swarm`. `Star Sticker Swarm` is the documented Star/Moon orbit-attach-ricochet weapon: orbit stars strike enemies, leave page stickers/nodes, and pop/ricochet with runtime upgrade scaling.
- Current passive pool is `candle_spark`, `cloud_seed`, `dream_thread`, `ribbon_spool`, and `moon_button`. All are Common with +10/+20/+30/+40/+50% level ladders. Candle Spark is Firelight/Light damage; Cloud Seed is Bloom/Wonder size; Dream Thread is Dream/Thread duration; Ribbon Spool is Thread/Star range; Moon Button is Moon/Echo cadence with the level-5 low-HP cadence bonus modeled in runtime state.
- Content implementation guardrail: initial/correction work for weapons, passive items, enemies, Page Events, chapters, Pagecraft materials, pets, characters, bosses, or unlocks must consult `PAGEBOUND_CODEX_GDD_v1_5.md` plus focused `design/gdd/*` docs and record `Design source consulted: ...` in bead/session-state.
- Run XP now uses the user-approved continuous Vampire Survivors-style curve scaled by 5, without level 20/40 walls. Base enemy XP is 5; level 1 -> 2 costs 25 XP; total XP to level 50 is 67,225.
- Spawn pressure now uses VS-inspired waves with an opening readability grace: min alive starts at 8, ramps to 25 by 1:00, then follows 25 -> 320 over the uncondensed 30-minute curve. Base spawn interval remains 1.0s -> 0.2s, target kill rate remains 1 -> 10 kills/sec, pressure spawns are suppressed above-minimum during the first minute, and max alive cap remains 350.
- Combat scale now matches the 1000 HP player baseline: Wax Imp base HP 200, Flicker Imp base HP 160, normal contact damage 60, starter Waxlight damage 100, Star Sticker base damage 80, Crownless Echo HP 2400, boss contact damage 160, boss XP reward 1000. Opening Wax Imp HP still equals two starting Waxlight hits.
- Draft/loadout spine: 3-card drafts, 5 weapon slots + 5 item slots. Normal drafts roll through `DraftChoicePicker`: 90% upgrade / 10% new gear, with 50/50 weapon/item category attempts and legality redirect. Page Event reward drafts guarantee a new gear card when legal. Draft cards include category, rarity, icon text, title, stat delta, description, level transition, and deduplicated tags; slot/evolution hint lines are intentionally omitted. Multiple level-up drafts queue one at a time.
- HUD is player-facing: top-middle run timer, hidden solo top-left party placeholders, top-right exclusive Page Event/boss banner, larger HP chip, dash recharge meter, bottom full-width XP percent bar, Level badge, Dog icon/feedback, and visible 5 weapon + 5 item loadout slots with levels. Escape/debug pause menu shows run stats, weapon/item levels, each weapon's applied upgrades, and Resume/Options/Quit controls.
- Dog support pet spawns as a visible non-colliding follower. Latest user clarification overrides stale aura docs: Dog has no pickup aura and does not instantly credit pickups at range. It extends player pickup reach by detecting eligible pickups in fetch range, paths to them with simple AI, then credits the pickup only when reached. T1 fetch range is 4.5m; T2 also accepts health pickups; T3 fetch range is 8.625m.
- Default non-XP-magnetism readable areas/durations were bumped by 50% for this prototype pass: weapon target ranges/mark sizes, Star Sticker page-sticker lifetime, Pagecraft activation padding/durations/pulse lifetime, Color Well radius, and Dog fetch ranges. XP pickup magnetism stays 3.0m.
- Page/map is widened for offscreen objectives: runtime half-extents are 32 x 20, page mesh is 64 x 40, camera follow half-extents are 28 x 16. Camera should keep player/current play readable.
- Color Well Page Event starts at 1:00, spawns at a random valid offscreen page position with reachable/nonblocked/nonclipping/non-boss-only state fields, exposes a HUD edge marker, lasts 60s, requires 15 enemy deaths by death position inside the circle, shows ring/fill progress, and cleans up world visuals plus active ID on completion/failure. Success opens a Page Event reward draft; failure gives no reward and run continues. Event and boss banners are exclusive.
- Crownless Echo (`crownless_echo`) is a killable mini-boss/finale tease scheduled around 3:30. If Color Well is active it queues until event resolution, then uses the top-right boss banner. Defeat drops XP/Color Motes only and does not open an in-run draft. The run no longer hard-stops at 5:00.
- Weapon stat model: `WeaponData` resources expose one editable base stat set (`base_damage`, `base_cooldown_seconds`, `base_mark_radius_meters`, `base_range_meters`). Weapon level is upgrade-count progress only; base stats do not auto-scale by level. Selected weapon upgrades apply additive modifiers from base stats. Waxlight Comet does direct hit plus impact splat/Pagecraft mark behavior; dash activation re-triggers Pagecraft trail/splat payoff.
- Verification on 2026-05-15: all smoke scripts under `tests/smoke/*.gd` passed before the opening-spawn correction. After the correction, targeted director/enemy/page-event/vertical-slice smoke checks passed and `git diff --check` passed. Bead `pagebound-84w` tracks the VS-style balance/UI correction pass; bead `pagebound-9xs` tracks the opening-spawn correction. Follow-up bead `pagebound-1c1` still tracks refactoring `src/runtime/first_playable_runtime.gd`, which remains over 800 lines.
